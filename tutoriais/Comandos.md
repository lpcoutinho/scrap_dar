# Comandos úteis

**Parar rodar em deploy:**

    ```bash
    uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    ```

    ```bash
    
    ```

    ```bash
    
    ```

    ```bash
    
    ```

    ```bash
    
    ```